[build]
  command = "npm install && npm run build"
  publish = "dist/ticketapp" # Ajusta si tu carpeta 'dist' es diferente (e.g., dist/ticketapp)

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200